# Google Analytics 4 measurement ID (e.g. G-XXXXXXXXXX)
NEXT_PUBLIC_GA_ID=

# Google AdSense publisher ID (e.g. ca-pub-XXXXXXXXXXXXXXXX) — leave blank until approved
NEXT_PUBLIC_ADSENSE_CLIENT=

# Backend API origin (used by server-side fetches; frontend fetches go through /api proxy)
BACKEND_URL=http://localhost:6004
