{
  "name": "padplay",
  "version": "0.1.0",
  "private": true,
  "description": "PadPlay — the leaderboard for games built for tablets, not stretched from phones.",
  "scripts": {
    "dev": "make dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "type-check": "pnpm -r type-check"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@9.0.0"
}
